<table class="table table--content table--mobile{% if table.mobileheaders %} table--mobile-headers{% endif %} {{ modifier_class }}">
  {% for row in table.rows %}
    <tr class="table__row table__row--{{ cycle(['odd', 'even'], loop.index0) }}">
      {% for item in row %}
        {% if table.mobile_single_column %}
          {% if loop.index == 1 %}
            <td class="table__cell">
          {% endif %}
          <div class="table__content" {% if table.mobileheaders %} data-mobile-label="{{ table.headers[loop.index0] }}"{% endif %}>
            {{ item }}
          </div>
        {% else %}
          {% if loop.index == 1 %}
            <td class="table__cell table__left">
              <div>{{ item }}</div>
            </td>
            <td class="table__cell table__right">
          {% endif %}
          {% if loop.index != 1 %}
            <div class="table__content" {% if table.mobileheaders %} data-mobile-label="{{ table.headers[loop.index0] }}"{% endif %}>
              {{ item }}
            </div>
          {% endif %}
        {% endif %}
      {% endfor %}
      </td>
    </tr>
  {% endfor %}
</table>
