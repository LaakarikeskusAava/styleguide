<table class="table table--content table--mobile{% if table.mobileheaders %} table--mobile-headers{% endif %} {{ modifier_class }}">
  {% for row in table.rows %}
    <tr class="table__row table__row--{{ cycle(['odd', 'even'], loop.index0) }}">
      {% for item in row %}
        {% if mobile_single_column %}
          {% set index = loop.index - 1 %}
          {% if forloop.index == 1 %}
            <td class="table__cell">
          {% endif %}
          <div class="table__content" {% if table.mobileheaders %} data-mobile-label="{{ table.headers[index] }}"{% endif %}>
            {{ item }}
          </div>
        {% else %}
          {% set index = forloop.index - 1 %}
          {% if loop.index == 1 %}
            <td class="table__cell table__left">
              <div>{{ item }}</div>
            </td>
            <td class="table__cell table__right">
          {% endif %}
          {% if loop.index != 1 %}
            <div class="table__content" {% if table.mobileheaders %} data-mobile-label="{{ table.headers[index] }}"{% endif %}>
              {{ item }}
            </div>
          {% endif %}
        {% endif %}
      {% endfor %}
      </td>
    </tr>
  {% endfor %}
</table>